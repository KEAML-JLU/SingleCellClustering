## Single Cell Clustering Pipeline

Uncompress `tar.gz` files in `dataset` directory.

Dimension Reducing and Clustering single cell data by `python run.py`

Plot results by `python plot_graph.py`



